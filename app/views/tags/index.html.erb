<h2>Available Ted Talk Categories </h2>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <pre>
        <b><%= link_to tag.name.strip.gsub("'","").capitalize, tag %></b>
      </pre>
    </li>
  <% end %>
</ul>
